from django.apps import AppConfig


class ArchitectuurConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.architectuur"
    verbose_name = "Architectuur (GEMMA)"
